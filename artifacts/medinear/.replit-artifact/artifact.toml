kind = "web"
previewPath = "/"
title = "MediNear"
version = "1.0.0"
id = "artifacts/medinear"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21898

[services.development]
run = "pnpm --filter @workspace/medinear run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/medinear", "run", "build" ]
publicDir = "artifacts/medinear/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21898"
BASE_PATH = "/"
